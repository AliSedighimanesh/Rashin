# Bilingual Rashin Homepage Only

Standalone static homepage for temporary deployment.

Files:

- `index.html`
- `styles.css`
- `script.js`

Open `index.html` directly or upload these three files to a static host.
